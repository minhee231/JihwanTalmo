from setuptools import setup, find_packages

setup(
    name="JihwanTModule",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        "CtrlJson",
        "Encryption"
    ],
)
